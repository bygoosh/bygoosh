name: bigoosh
description: A minimal video sharing platform MVP.
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^2.15.1
  firebase_auth: ^4.10.0
  cloud_firestore: ^4.9.3
  firebase_storage: ^11.2.6
  video_player: ^2.7.0
  file_picker: ^6.1.1
  provider: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
